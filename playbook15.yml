---
- name: Implementing loops
  hosts: all
  tasks:
    - name: Install tree s/w's
      apt:
        name: "{{item}}"
        state: present
        update_cache: no
      with_items:
        - tree
        - git
        - maven
    
...
